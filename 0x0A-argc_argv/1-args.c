#include <stdio.h>

/* A program that prints the name of an argument passed to it
 * print a number followed by a new line
 * argc, 
 * argv, 
 */
int main(argc, char *argv[] __attribute__((__unused__)) )
{	
	printf("%d\n", argc);

	return (0);
}
